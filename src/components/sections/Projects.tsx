import { ArrowUpRight } from 'lucide-react'
import { projects } from '../../data/content'
import { Section } from '../layout/Section'
import { SectionTitle } from '../ui/SectionTitle'
import { Reveal } from '../ui/Reveal'

export const Projects = () => <Section id="work"><SectionTitle eyebrow="01 / Selected Work" description="A selection of projects spanning computer vision, generative AI, RAG systems, data analytics, and application engineering.">Things I've built.</SectionTitle><div className="grid gap-4 md:grid-cols-2">{projects.map((project) => <Reveal key={project.name} className={project.featured ? 'md:col-span-2' : ''}><article className={`group flex h-full flex-col overflow-hidden rounded-2xl border border-line bg-card p-6 transition duration-300 hover:-translate-y-1 hover:border-line-strong hover:shadow-card ${project.featured ? 'md:p-8' : ''}`}><div className="flex items-start justify-between gap-5"><span className="font-mono text-[0.67rem] tracking-[0.1em] text-accent">{project.number}</span><span className="rounded-full border border-line px-2.5 py-1 font-mono text-[0.62rem] text-muted">{project.status}</span></div><h3 className={`mt-8 font-display tracking-[-0.035em] text-paper ${project.featured ? 'text-4xl md:text-5xl' : 'text-3xl'}`}>{project.name}{project.arabicName && <span className="ml-2 text-accent">{project.arabicName}</span>}</h3><p className="mt-4 max-w-3xl text-sm leading-7 text-soft">{project.description}</p><div className="mt-6 flex flex-wrap gap-2">{project.tags.map((tag) => <span key={tag} className="rounded-md border border-line px-2.5 py-1.5 font-mono text-[0.63rem] text-soft">{tag}</span>)}</div><a href={project.link} target="_blank" rel="noreferrer" className="mt-8 inline-flex items-center gap-2 self-start font-mono text-[0.68rem] uppercase tracking-[0.1em] text-accent transition hover:text-paper">{project.linkLabel}<ArrowUpRight size={14} className="transition group-hover:translate-x-0.5 group-hover:-translate-y-0.5" /></a></article></Reveal>)}</div></Section>
